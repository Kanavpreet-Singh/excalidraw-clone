# ==============================
# Frontend
# ==============================
server {
    listen 80;
    server_name staging.structura-fe.testingdemo.me;

    # Redirect HTTP to HTTPS after certificate is issued
    # For now, just proxy to Node.js
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}

# ==============================
# Backend API
# ==============================
server {
    listen 80;
    server_name staging.structura-http.testingdemo.me;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}

# ==============================
# WebSocket
# ==============================
server {
    listen 80;
    server_name staging.structura-ws.testingdemo.me;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'Upgrade';
        proxy_set_header Host $host;
    }
}
